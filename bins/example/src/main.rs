//! Example composition root — the template's only binary, and the single
//! place where the hexagonal layers are wired together and started.

fn main() {
    // Replace this stub: construct the outgoing adapters (e.g.
    // `seaorm-postgres`), inject them into the application layer, and serve
    // traffic through the incoming adapter (`http-axum`).
}
